# Backend en Nest
```
docker compose up -d
```
Copiar el ```.env.template``` y renombrarlo a ```.env```

## Comandos usados
```
docker compose up -d
nest g resource auth
npm i @nestjs/mongoose mongoose
npm i @nestjs/config
```